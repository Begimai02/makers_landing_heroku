import React from 'react';
import './Button.css';

const Button = () => {
    return (
        <div className="btn-wrapper">
            <button className="btn btn_eight hero_btn btn_four">Оставить заявку</button>
        </div>
    );
};

export default Button;